name: 🚀 Build on Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🏗️ Build Project
        run: npm run build

      - name: 🔐 Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      - name: ☁️ Sync dist folder to S3 PORTAL
        run: |
          aws s3 sync dist/ s3://webapps.sportzlytics.com/BMS/ --delete

      # - name: 📄 Upload ReleaseNotes.txt to S3 PORTAL
      #   run: |
      #     aws s3 cp ReleaseNotes.txt s3://webapps.sportzlytics.com/BMS/ReleaseNotes.txt
